version: "3.9"

services:
  fastapi_app:
    build: .
    container_name: fastapi_app
    ports:
      - "8000:8000"
    volumes:
      - D:\Geo Makanii\SGS project\Website Backend\Fast API\api_files:/app/app/static
    env_file:
      - app/.env
    restart: always
